import React from 'react'

type Props = {}

const HelpandSupport = (props: Props) => {
  return (
    <div>
        <h1> Include settings for managing document security and privacy options, such as access controls and encryption settings.</h1>-
    </div>
  )
}

export default HelpandSupport